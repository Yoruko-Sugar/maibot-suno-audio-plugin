# 更新日志

## 0.1.5

### 细节

- 同一任务返回多条候选音轨时只发送一条汇总报告，再依次发送音频。
- 报告移除任务 ID、结果序号和时长；歌曲显示“歌名”，纯音乐显示“音乐名”。
- 同名候选音轨在报告中合并显示，避免重复信息。

## 0.1.4

### 细节

- 供应商先返回 `completed`、但 `music[]` 尚未同步时继续轮询，不再立即判定协议失败。
- 兼容供应商将完整任务包装在顶层 `data`、并直接返回 `data.result.music[]` 的实际响应结构。
- 新增完成结果同步宽限时间配置，默认等待 90 秒。
- 控制台记录不含结果内容的响应字段结构，便于定位供应商响应差异。
- 自动恢复此前因“完成但没有 music[]”而停止跟踪的旧任务，且不会重新提交计费任务。

## 0.1.3

### 细节

- 兼容 API Mart 实际返回的 `processing` 任务状态，并继续按生成中任务轮询。
- 保留供应商原始状态用于控制台日志，本地任务状态统一记录为 `pending`。
- 升级后自动恢复此前因 `processing` 被误判而停止跟踪的任务，避免重新生成和重复计费。

## 0.1.2

### 主要功能

- 完整音乐和歌曲新增 `voice` 发送模式，可直接作为平台语音消息发送。

### 细节

- 语音模式继续受配置中的 Base64 文件大小上限约束，发送失败时保留已生成结果供用户重发。

## 0.1.1

### 细节

- 修复“默认人声性别”下拉框包含空值，导致配置页无法渲染的问题。
- 新增 `auto` 选项表示不指定人声性别，并兼容旧配置中的空字符串。

## 0.1.0

### 主要功能

- 新增音乐、纯音乐、自定义歌曲、歌词和音效生成。
- 新增自然语言 Tool 触发与结构化提示词优化。
- 新增异步任务持久化、重启恢复、历史查询和结果重发。

### 细节

- 完整歌曲默认以文件发送，短音效可配置为语音消息。
- API Mart 错误、任务失败、跟踪超时和发送失败分别记录。
- 提示词优化模型从 MaiBot 当前全部模型任务中动态选择。
